<article>
  <a href="/dashboard/{{id}}">
    <header>
      <strong>{{title}}</strong>
      Posted by {{user.username}} on {{format_date created_at}}
    </header>
  </a>
  <p>{{content}}</p>
</article>